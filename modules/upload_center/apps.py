from django.apps import AppConfig

class UploadCenterConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'modules.upload_center'  # Must match the path in INSTALLED_APPS
